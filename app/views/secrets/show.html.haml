:css
  body {
    background-color: #333333;
    padding: 64px;
    margin: 0;
  }

  p {
    color: white;
    font-family: 'Roboto', sans-serif;
  }

%p You should not be here.. where we keep things that no one should know.
= form_tag('/secret', method: :patch) do
  = password_field_tag :secret
  = submit_tag '➡'

